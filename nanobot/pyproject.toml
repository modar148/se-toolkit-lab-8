[project]
name = "nanobot"
version = "0.1.0"
description = "Nanobot agent for Lab 8"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
